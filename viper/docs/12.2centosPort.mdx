---
id: 12_2_introduce
title: 12.2 Centos7端口设置
sidebar_label: 12.2 Centos7端口设置
---

import useBaseUrl from "@docusaurus/useBaseUrl";

### 1、开放端口
```shell

firewall-cmd --zone=public --add-port=5672/tcp --permanent   # 开放5672端口

firewall-cmd --zone=public --remove-port=5672/tcp --permanent  #关闭5672端口

firewall-cmd --reload   # 配置立即生效
```
 

### 2、查看防火墙所有开放的端口
```shell
firewall-cmd --zone=public --list-ports
```
 

### 3.、关闭防火墙

```shell
systemctl stop firewalld.service
```
 

### 4、查看防火墙状态
```shell
 firewall-cmd --state
```
 

### 5、查看监听的端口
```shell
netstat -lnpt
```
<img src={useBaseUrl("img/netstat01.jpg")} />

:::caution 特别注意
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;`Centos7` 默认没有 `netstat` 命令，需要安装 `net-tools` 工具，`yum install -y net-tools`
:::

 

 

### 6、检查端口被哪个进程占用
```shell
netstat -lnpt |grep 6660
```
<img src={useBaseUrl("img/netstat02.jpg")} />

 

### 7、查看进程的详细信息
```shell
ps 2093
```
<img src={useBaseUrl("img/netstat03.jpg")} />

 

### 8、中止进程
```shell
kill -9 2093
```